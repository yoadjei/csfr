# CSFR reproducibility package — exact versions used for the released results.
# The sweep harness also records these into each run's run_metadata.json.
#
# Note: the primary DFRWS sweep (results/csfr_sweep_2d_v2/) predates automated
# environment capture; the external-validation sweep and all analysis and
# figure scripts were run under the versions pinned below.
numpy==2.5.1
scipy==1.17.1
torch==2.13.0
scikit-image==0.26.0
scikit-learn==1.9.0
opencv-python-headless==4.11.0.86
matplotlib==3.11.0
Pillow==12.3.0
PyYAML==6.0.3
psutil==7.2.2
